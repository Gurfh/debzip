# Keys

Run `./scripts/generate-gpg-key.sh` to create a local signing key and export `debzip-public-key.asc`.

For production, protect the private key carefully and consider using a dedicated signing process.
